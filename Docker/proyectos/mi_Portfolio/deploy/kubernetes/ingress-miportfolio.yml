apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-miportfolio
  namespace: namespace-miportfolio
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: portfoliobelen.38.242.240.106.nip.io  # <--- CAMBIA ESTO
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: service-miportfolio
                port:
                  number: 80