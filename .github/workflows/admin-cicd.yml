name: Deploy Admin Panel to Production

on:
  push:
    branches:
      - main
    paths:
      # Se activa si cambias el código o los manifiestos del panel admin
      - 'Docker/proyectos/Ukiyo_admin/**'

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

    - name: Build and Push Docker Image
      uses: docker/build-push-action@v5
      with:
        context: ./Docker/proyectos/Ukiyo_admin
        file: ./Docker/proyectos/Ukiyo_admin/Dockerfile
        push: true
        tags: ${{ secrets.DOCKER_HUB_USERNAME }}/ukiyo-admin:latest