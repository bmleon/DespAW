apiVersion: v1
kind: Service
metadata:
  name: {{ include "portfolio-bmleon.fullname" . }}
  labels:
    {{- include "portfolio-bmleon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "portfolio-bmleon.selectorLabels" . | nindent 4 }}
