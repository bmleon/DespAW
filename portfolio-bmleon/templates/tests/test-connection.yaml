apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portfolio-bmleon.fullname" . }}-test-connection"
  labels:
    {{- include "portfolio-bmleon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portfolio-bmleon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
